# ── Core runtime ───────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
python-multipart>=0.0.9
pydantic>=2.6,<3.0
sqlalchemy>=2.0,<3.0
anthropic>=0.40
openai>=1.30  # Whisper STT only; the agentic planner runs on Anthropic
python-dotenv>=1.0
loguru>=0.7

# ── Optional: barcode scan (/scan) + OCR. Require native libs
#    (zbar, tesseract). App runs without them — imports are guarded. ──
pytesseract>=0.3.10
opencv-python>=4.9
pyzbar>=0.1.9
Pillow>=10.0

# ── Dev / test ─────────────────────────────────────────────────────
pytest>=8.0
httpx>=0.27

# Note: authentication (JWT + password hashing) is implemented with the
# Python standard library — no third-party crypto dependency to patch.
